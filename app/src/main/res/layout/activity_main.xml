<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/colorPrimary"
        android:gravity="center"
        android:titleTextColor="@android:color/white"
        android:title="Scorekeeper"
        android:elevation="4dp" />

    <!-- Contenedor para el Equipo 1 -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <!-- Nombre del Equipo 1 -->
        <TextView
            android:id="@+id/team1_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Team 1"
            android:textAppearance="@style/TextAppearance.AppCompat.Headline"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="8dp" />

        <!-- Botón para decrementar la puntuación del Equipo 1 -->
        <ImageButton
            android:id="@+id/decreaseTeam1"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:background="@drawable/button_background"
            android:contentDescription="@string/minus_button"
            android:src="@drawable/ic_minus"
            android:onClick="decreaseScore" />

        <!-- Puntuación del Equipo 1 -->
        <TextView
            android:id="@+id/score_1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="0"
            android:textAppearance="@style/TextAppearance.AppCompat.Display3" />

        <!-- Botón para incrementar la puntuación del Equipo 1 -->
        <ImageButton
            android:id="@+id/increaseTeam1"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="@drawable/button_background"
            android:contentDescription="@string/plus_button"
            android:src="@drawable/ic_plus"
            android:onClick="increaseScore" />
    </RelativeLayout>

    <!-- Contenedor para el Equipo 2 -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <!-- Nombre del Equipo 2 -->
        <TextView
            android:id="@+id/team2_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Team 2"
            android:textAppearance="@style/TextAppearance.AppCompat.Headline"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="8dp" />

        <!-- Botón para decrementar la puntuación del Equipo 2 -->
        <ImageButton
            android:id="@+id/decreaseTeam2"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:background="@drawable/button_background"
            android:contentDescription="@string/minus_button"
            android:src="@drawable/ic_minus"
            android:onClick="decreaseScore" />

        <!-- Puntuación del Equipo 2 -->
        <TextView
            android:id="@+id/score_2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="0"
            android:textAppearance="@style/TextAppearance.AppCompat.Display3" />

        <!-- Botón para incrementar la puntuación del Equipo 2 -->
        <ImageButton
            android:id="@+id/increaseTeam2"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="@drawable/button_background"
            android:contentDescription="@string/plus_button"
            android:src="@drawable/ic_plus"
            android:onClick="increaseScore" />
    </RelativeLayout>

</LinearLayout>
